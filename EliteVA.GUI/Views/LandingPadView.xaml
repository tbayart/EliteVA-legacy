<Window x:Class="EliteVA.GUI.Views.LandingPadView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EliteVA.GUI"
        xmlns:views="clr-namespace:EliteVA.GUI.Views"
        xmlns:iconpacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        mc:Ignorable="d" Topmost="True" WindowStyle="None" Background="Transparent"
        Title="Landing pads" Height="500" Width="500" ResizeMode="NoResize"
        DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Grid>
        <Image Name="LandingPadsImage" Source="pack://application:,,,/EliteVA.GUI;component/Medias/LandingPads_Coriolis.png" Stretch="UniformToFill" />
        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center"
                   Text="{Binding Title}" Foreground="Black" FontSize="20"/>
        <Canvas>
            <iconpacks:PackIconMaterial Name="LandingPadIcon" Kind="SelectionEllipseArrowInside" Spin="True" Foreground="Coral"
                                        Width="40" Height="40" VerticalAlignment="Center" />
        </Canvas>
    </Grid>
</Window>
